let custoDeFabrica, valorTotal;

custoDeFabrica = parseFloat(prompt("Qual é o custo de fábrica? "));
valorTotal = custoDeFabrica + (custoDeFabrica*(28/100)) + (custoDeFabrica*(45/100));
Escreva ("O custo final do consumidor é R$ " + valorTotal);
