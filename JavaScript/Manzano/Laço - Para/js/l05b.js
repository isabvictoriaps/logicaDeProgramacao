let numero,contadora;

numero = parseInt(prompt("De qual número você quer ver a tabuada? "));

for(let contadora = 1; contadora <=10; contadora++){
    alert(numero+" x "+contadora+" = "+numero*contadora)
}